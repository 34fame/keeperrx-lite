import {
   getFirestoreObjects,
   saveFirestoreObject,
   deleteFirestoreObject,
} from './firestore'
import {
   firebaseAuth,
   facebookProvider,
   githubProvider,
   googleProvider,
} from './authentication'

export {
   firebaseAuth,
   facebookProvider,
   githubProvider,
   googleProvider,
   getFirestoreObjects,
   saveFirestoreObject,
   deleteFirestoreObject,
}
